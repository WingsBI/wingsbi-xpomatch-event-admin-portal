# XPO Match Event Admin API Configuration
NEXT_PUBLIC_API_BASE_URL=https://xpomatch-test-event-admin-api.azurewebsites.net

NODE_ENV=test

# Contact Email (already configured in code)
CONTACT_EMAIL=contact@wingsbi.com

# Feature Flags
NEXT_PUBLIC_ENABLE_DEMO_MODE=false